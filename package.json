{
  "name": "clarity",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run postbuild && wrangler deploy --bundle",
    "postbuild": "node -e \"const fs=require('fs'); ['dist/clarity_worker/wrangler.json','dist/clarity/wrangler.json'].forEach(p=>{ if(fs.existsSync(p)){ let c=JSON.parse(fs.readFileSync(p,'utf8')); delete c.no_bundle; fs.writeFileSync(p, JSON.stringify(c)); console.log('Fixed no_bundle in', p); } })\""
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.0.0",
    "@cloudflare/workers-types": "^5.20260903.1",
    "@vitejs/plugin-react": "^4.4.0",
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
